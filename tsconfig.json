{
  "//": "Root config for editor tooling. Each workspace has its own tsconfig extending @ramps/config/tsconfig.base.json. References are added here as composite workspaces land.",
  "files": [],
  "references": [{ "path": "packages/schemas" }]
}
