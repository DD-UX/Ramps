# Workspace packages are linked from the monorepo, not the registry.
link-workspace-packages=true
# Keep the dependency graph honest: no phantom hoisted deps.
shamefully-hoist=false
# Consistent, deterministic installs.
save-exact=true
